// const LZMA = require('lzma')
// const myLzma = new LZMA()

// export const compressFile = (content) => new Promise((resolve, reject) => {
//   myLzma.compress(content, 5, (result, error) => {
//     resolve(result)
//   })
// })

// export const decompressFile = (byteArray) => new Promise((resolve, reject) => {
//   myLzma.decompress(byteArray, (result, error) => {
//     resolve(result)
//   })
// })
